---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Projects — Joe Morrison" description="Selected projects">
  <h1 class="title">Projects</h1>
  <p class="desc">A curated set of things I’ve built. Short writeups, tools, results.</p>

  <section class="stack">
    <article id="noise" class="card">
      <div class="top">
        <h2 class="h2">NOISE — Engine Sound Classifier</h2>
        <span class="tag">Software / ML</span>
      </div>

      <p class="body">
        A lightweight app that listens to engine audio and classifies patterns into categories that help narrow down diagnostic paths.
      </p>

      <div class="meta">
        <div class="mini"><div class="k">Tech</div><div class="v">Python, Teachable Machine / TFLite</div></div>
        <div class="mini"><div class="k">Focus</div><div class="v">Audio classification + UX</div></div>
        <div class="mini"><div class="k">Outcome</div><div class="v">Prototype classifier + continuous mic input</div></div>
      </div>

      <div class="actions">
        <a class="btn primary" href="#">Demo (https://youtu.be/fC-c-fhDy5Y?si=uIffJqDpDfnrPysw)</a>
        <a class="btn ghost" href="#">GitHub (https://github.com/joeeagledrone)</a>
      </div>

      <p class="note">Next: screenshots/GIFs, model details, metrics, and “how to reproduce.”</p>
    </article>
  </section>

  <style>
    .title{ font-size: 34px; margin:0; letter-spacing:-0.02em; }
    .desc{ margin:10px 0 0; color: rgba(229,231,235,.70); max-width: 720px; line-height:1.6; }
    .stack{ margin-top: 22px; display:grid; gap: 14px; }
    .card{ border-radius: 18px; border:1px solid rgba(255,255,255,.10); background: rgba(255,255,255,.05); padding: 18px; }
    .top{ display:flex; flex-wrap:wrap; align-items:center; justify-content:space-between; gap:10px; }
    .h2{ margin:0; font-size: 18px; }
    .tag{ font-size: 12px; color: rgba(229,231,235,.70); border:1px solid rgba(255,255,255,.10); background: rgba(255,255,255,.05); padding: 4px 10px; border-radius: 999px; }
    .body{ margin: 12px 0 0; color: rgba(229,231,235,.72); line-height:1.65; }
    .meta{ margin-top: 14px; display:grid; gap: 10px; grid-template-columns: 1fr; }
    @media (min-width: 720px){ .meta{ grid-template-columns: repeat(3, 1fr); } }
    .mini{ border-radius: 14px; border:1px solid rgba(255,255,255,.10); background: rgba(0,0,0,.20); padding: 12px; }
    .k{ font-size: 12px; color: rgba(229,231,235,.55); }
    .v{ margin-top: 6px; font-size: 13px; color: rgba(229,231,235,.82); }
    .actions{ margin-top: 14px; display:flex; gap: 10px; flex-wrap:wrap; }
    .btn{ display:inline-flex; padding: 10px 14px; border-radius: 14px; font-size: 14px; font-weight: 650; text-decoration:none; border:1px solid rgba(255,255,255,.12); }
    .primary{ background:#fff; color:#000; border-color: transparent; }
    .primary:hover{ opacity:.92; }
    .ghost{ background: rgba(255,255,255,.06); color: rgba(229,231,235,.92); }
    .ghost:hover{ background: rgba(255,255,255,.10); }
    .note{ margin: 14px 0 0; font-size: 13px; color: rgba(229,231,235,.55); }
  </style>
</Layout>
